- model_class = JobseekerMentor
.page-header
  %h1=t '.title', :default => model_class.model_name.human.pluralize
%table.table.table-striped
  %thead
    %tr
      %th= model_class.human_attribute_name(:id)
      %th= model_class.human_attribute_name(:user_id)
      %th= model_class.human_attribute_name(:job_title)
      %th= model_class.human_attribute_name(:support)
      %th= model_class.human_attribute_name(:job_description)
      %th= model_class.human_attribute_name(:created_at)
      %th=t '.actions', :default => t("helpers.actions")
  %tbody
    - @jobseeker_mentors.each do |jobseeker_mentor|
      %tr
        %td= link_to jobseeker_mentor.id, jobseeker_mentor_path(jobseeker_mentor)
        %td= jobseeker_mentor.user_id
        %td= jobseeker_mentor.job_title
        %td= jobseeker_mentor.support
        %td= jobseeker_mentor.job_description
        %td=l jobseeker_mentor.created_at
        %td
          = link_to t('.edit', :default => t("helpers.links.edit")), edit_jobseeker_mentor_path(jobseeker_mentor), :class => 'btn btn-mini'
          = link_to t('.destroy', :default => t("helpers.links.destroy")), jobseeker_mentor_path(jobseeker_mentor), :method => :delete, :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-mini btn-danger'

= link_to t('.new', :default => t("helpers.links.new")), new_jobseeker_mentor_path, :class => 'btn btn-primary'
