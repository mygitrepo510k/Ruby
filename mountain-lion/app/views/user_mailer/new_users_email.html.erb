<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN" "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">
<html xmlns="http://www.w3.org/1999/xhtml">
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <title>Untitled Document</title>
    <style type="text/css">
      body {font-family: Arial, Helvetica, sans-serif}
      .style1 {font-family: Arial, Helvetica, sans-serif}
      .style4 {font-size: 10px}
      img#i_thumb, img#thumb {
        height: 64px;
        width: 64px;
      }
    </style>
  </head>

  <body>
    <table width="535" border="0" align="center" cellpadding="0" cellspacing="0">
      <tr>
        <td align="center"><img src="<%= asset_url('header-logo.gif') %>" width="317" height="75" /></td>
      </tr>
      <tr>
        <td><hr size="1" /></td>
      </tr>
      <tr>
        <td>&nbsp;</td>
      </tr>
      <tr>
        <td class="style1"><p>Hello <span><strong><em><%= @user.username %></em></strong></span>,<br />
          <br />
          Here's a list of members who recently signed up to HyeDating.com:</p>

          <table width="100%" border="0" cellspacing="0" cellpadding="4">
            <tr>
              <% @new_users.from(0).take(5).each do |user| %>
                <%= render 'new_user', user: user %>
              <% end %>
            </tr>

            <tr>
              <td colspan="5" align="left" valign="top">&nbsp;</td>
            </tr>
            <tr>
              <% @new_users.from(5).take(5).each do |user| %>
                <%= render 'new_user', user: user %>
              <% end %>
            </tr>
          </table>
          <%= render 'footer', token: @user.unsubscribe_token %>
          <tr>
            <td>&nbsp;</td>
          </tr>
        </table>
      </body>
    </html>


