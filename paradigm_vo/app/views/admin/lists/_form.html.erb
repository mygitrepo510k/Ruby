<%= form_for([:admin, @list]) do |f| %>
  <% if @list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@list.errors.count, "error") %> prohibited this list from being saved:</h2>

      <ul>
      <% @list.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <p><%= f.label :name %><br></p>
    <p><%= f.text_field :name %><br></p>
    <p><%= f.label :recipient %><br></p>
    <p><%= f.text_field :recipient %><br></p>
    <p><%= f.submit %></p>
  </div>
<% end %>
